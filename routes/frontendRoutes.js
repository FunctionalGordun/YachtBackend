const express = require('express');
const router = express.Router();
// const { addFrontendBookingEvent, getPayment, addFrontendGroomingEvent } = require('../controller/frontendBookingController');

// router.post('/', addFrontendBookingEvent);

// router.post('/payment', getPayment);

// router.post('/grooming', addFrontendGroomingEvent);

module.exports = router;
